# 실습 ①: 첫 Pod 배포
# 적용:  kubectl apply -f nginx-pod.yaml
# 확인:  kubectl get pods -o wide

apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1.25
    ports:
    - containerPort: 80
