services:
  web:
    build: .              # Dockerfile로 빌드
    ports:
      - "8000:5000"
    environment:
      - APP_ENV=development
    restart: unless-stopped
    depends_on:
      - redis
  redis:
    image: redis:7-alpine  # 공식 이미지 사용
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
    volumes:
      - redisdata:/data

volumes:
  redisdata:
